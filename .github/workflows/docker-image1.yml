name: Add character ↉ (U+2189) to the Typeface

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag bricolage:latest

      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: ~/.cache
          key: ${{ runner.os }}-build-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-build-

      - name: Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: build-artifacts
          path: ./build

          run: docker build . --file docker/Dockerfile --tag bricolage:latest
